# Database (Supabase PostgreSQL)
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"

# NextAuth.js
NEXTAUTH_URL="https://infinitrade.ro"
NEXTAUTH_SECRET="your-random-32-character-secret-here"

# =============================================================================
# RATE LIMITING (REQUIRED FOR PRODUCTION)
# =============================================================================
# Create free Redis at https://upstash.com
# Without this, rate limiting will use in-memory store (NOT suitable for serverless!)
UPSTASH_REDIS_REST_URL="https://xxx.upstash.io"
UPSTASH_REDIS_REST_TOKEN="AXxx..."

# =============================================================================
# DATABASE SEED CREDENTIALS (REQUIRED FOR npm run db:seed)
# =============================================================================
# Password requirements:
#   - Minimum 12 characters
#   - At least one uppercase letter (A-Z)
#   - At least one lowercase letter (a-z)
#   - At least one number (0-9)
#   - At least one special character (!@#$%^&* etc.)
#
# NEVER commit real passwords to version control!
ADMIN_SEED_PASSWORD="YourSecureAdminPassword123!"
SALES_SEED_PASSWORD="YourSecureSalesPassword123!"

# =============================================================================
# OPTIONAL SERVICES
# =============================================================================
# Anthropic (for AI features if any)
ANTHROPIC_API_KEY="sk-ant-..."

# Resend (for transactional emails)
RESEND_API_KEY="re_..."

# Site URL (for canonical URLs, sitemap)
NEXT_PUBLIC_SITE_URL="https://infinitrade.ro"

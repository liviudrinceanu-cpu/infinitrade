import fs from 'node:fs'; import { loadDataDir } from './gates/_lib/loader.mjs';
const kw=JSON.parse(fs.readFileSync('.planning/brands-500/kw-volumes-ro-2026-09.json','utf8')); const data=kw.data||kw;
const norm=s=>s.toLowerCase().replace(/[^a-z0-9]+/g,' ').trim(); const byName={}; for(const [k,v] of Object.entries(data)) byName[norm(k)]=v;
const amb=new Set(['ares','castel','dresser','gates','stanley','orion','vesta','seneca','marathon','binder','sola','tone','mono','flux','crane','imo','heb','jax','atb','aem','regal','viking','rotor','brook','leroy','nord','armatura','watts','omega','delta','alpha','sigma','apex','titan','atlas','eagle','pioneer','victor','star','max','pro','elite','prime','nova','summit','union','national','general','standard','universal','continental','pacific','atlantic','baltic','danube','carpat','end','mp','elmo','lutz','yamada','rossi','marelli','cantoni','hansen','sumitomo','brevini','nicolini','fimet','mgm','teco','vem','kamat','maag','shell','mobil','castrol','total','carrier','trane','daikin','hilti','bosch','makita','dewalt','stanley','black-decker']);
const conf=JSON.parse(fs.readFileSync('.planning/brands-500/brands-new-confirmed.json','utf8')); const confBySlug=Object.fromEntries(conf.map(b=>[b.slug,b]));
const { importFile, cleanup } = loadDataDir(process.cwd()); const idx = await importFile('allBrandsIndex.js'); cleanup();
const out={}; let n=0;
for (const b of idx.allBrandsUnified) { const c=confBySlug[b.simpleSlug]; let v = c && c.kwVolumeRO!=null ? c.kwVolumeRO : (byName[norm(b.name)] ?? byName[b.simpleSlug.replace(/-/g,' ')]); if (c && c.kwAmbiguous) v=null; if (v!=null && !amb.has(b.simpleSlug)) { out[b.simpleSlug]=v; n++; } }
const lines=Object.entries(out).sort().map(([k,v])=>'  '+JSON.stringify(k)+': '+v+',').join('\n');
fs.writeFileSync('src/data/brandDemand.js', `// GENERATED (D-2026-09-22) from .planning/brands-500/kw-volumes-ro-2026-09.json\n// (Google Ads Keyword Planner, RO, Sep 2025–Aug 2026, avg. monthly searches for the brand name).\n// Used ONLY for ordering brands inside category pages; never rendered as a figure.\n// Generic/ambiguous names are excluded (their volume is not the industrial brand's).\nexport const BRAND_DEMAND = {\n${lines}\n};\n\nexport function getBrandDemand(slug) {\n  return BRAND_DEMAND[slug] || 0;\n}\n`);
console.log('brands',idx.allBrandsUnified.length,'mapped',n, ['grundfos','abb','siemens','wilo','knipex','ksb'].map(k=>k+':'+out[k]).join(' '));

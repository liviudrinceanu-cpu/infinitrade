// Fixture for G8 (F0-07): the forbidden-claim family — MUST be flagged as a
// candidate (severity MAJOR, status "needs_model").
export const brandContent = {
  acme: {
    overview: 'Suntem distribuitor autorizat Acme pentru România, cu reprezentanță locală și service autorizat.',
  },
};

export default brandContent;
